import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.contrue.miramate',
  appName: 'miramate-v2-client',
  webDir: 'dist'
};

export default config;
